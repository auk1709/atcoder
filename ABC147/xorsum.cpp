#include <cstdio>
#include <cstdlib>
#include <bitset>

using namespace std;

int main(){
  int n;
  scanf("%d", &n);
  int *a;
  a = (int *)malloc(sizeof(int) * n);
  for (int i = 0; i < n;i++){
    scanf("%d", &a[i]);
  }


}