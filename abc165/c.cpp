#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, q, e, f, g, h;
  cin >> n >> m >> q;

  vector<int> a;
  vector<int> b;
  vector<int> c;
  vector<int> d;
  for (int i = 0; i < q; i++) {
    cin >> e >> f >> g >> h;
    a.push_back(e);
    b.push_back(f);
    c.push_back(g);
    d.push_back(h);
  }
  vector<int> A(n);
  for (int bit = 0; bit < (1 << n); ++bit) {
  }
}